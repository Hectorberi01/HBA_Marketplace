<Project Sdk="Microsoft.NET.Sdk">

  <!--
    ══════════════════════════════════════════════════════════════════════════
    CE PROJET N'AVAIT AUCUN PAQUET EF : IL N'AVAIT RIEN À PERSISTER.

    Son unique fichier enregistrait un `ConcurrentDictionary` en singleton.
    Depuis ce lot il porte `DriverDbContext`, ses configurations, son dépôt et
    sa migration initiale — d'où les quatre paquets EF, alignés sur ceux de
    `HBA.Delivery.Core.Infrastructure` et de `HBA.Delivery.Pricing.Infrastructure`.

    `Microsoft.EntityFrameworkCore.Design` est nécessaire aux attributs
    `[DbContext]` / `[Migration]` des migrations écrites à la main : sans lui,
    `Microsoft.EntityFrameworkCore.Infrastructure.DbContextAttribute` n'est pas
    résolu et le dossier `Migrations` ne compile pas.
    ══════════════════════════════════════════════════════════════════════════
  -->
  <ItemGroup>
    <ProjectReference Include="..\HBA.Delivery.Driver.Domain\HBA.Delivery.Driver.Domain.csproj" />
    <ProjectReference Include="..\HBA.Delivery.Driver.Application\HBA.Delivery.Driver.Application.csproj" />
    <ProjectReference Include="..\..\..\..\..\shared\common\HBA.Shared.Infrastructure\HBA.Shared.Infrastructure.csproj" />
    <ProjectReference Include="..\..\..\..\..\shared\common\HBA.Shared.IntegrationEvents\HBA.Shared.IntegrationEvents.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Relational" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
  </ItemGroup>

</Project>
