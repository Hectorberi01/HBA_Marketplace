apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - statefulset.yaml
  # - secret.yaml   # posé par kubectl, jamais par kustomize (§12)
  # - job-buckets.yaml   # appliqué à la main APRÈS que MinIO tourne (Job immuable)
