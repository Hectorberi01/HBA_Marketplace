apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Le data plane du §22 (phase P1). Kafka vit dans `../kafka` : c'est un opérateur
# tiers (Strimzi), et son CR n'a pas la même forme que ces StatefulSets.
resources:
  - postgres
  - redis
  - minio
