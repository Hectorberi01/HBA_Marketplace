apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CE DOSSIER N'EST PAS UN DÉPLOIEMENT. Il est repris par chaque service voisin
# avec un `namePrefix` — `identity-` + `service` donne `identity-service`.
resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - pdb.yaml
  - hpa.yaml
